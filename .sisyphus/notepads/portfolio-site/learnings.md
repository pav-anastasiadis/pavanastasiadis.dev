# Learnings — portfolio-site

## [Init] Architecture Decisions

- Package manager: pnpm exclusively
- Next.js App Router, TypeScript strict, Tailwind CSS v4 (CSS-first @theme, NO tailwind.config.js)
- @next/mdx for MDX — requires remark-frontmatter + remark-mdx-frontmatter for YAML frontmatter support
- gray-matter is FORBIDDEN — MDX frontmatter via remark-mdx-frontmatter only
- ESLint flat config (eslint.config.mjs), eslint-config-prettier MUST be last
- Husky v9: npx husky init, prepare script is "husky"
- Fonts: next/font/google → CSS variables (--font-pixel, --font-terminal) → @theme references
- File discovery: fs.readdirSync (not globby)
- Dynamic imports: await import(`@/content/blog/${slug}.mdx`) for blog posts
- Plugin order: remark plugins first (frontmatter → GFM), then rehype (slug → pretty-code)
- RSS: route handler at app/rss.xml/route.ts, must XML-escape all dynamic content
- Demo isolation: next/dynamic with ssr: false, CSS Modules or inline styles per demo
- prefers-reduced-motion required for all CRT/scanline animations

## [Task 1] Scaffold Findings

- Next.js version installed: 16.2.1
- Tailwind version: v4.2.2 — CSS-first confirmed (no tailwind.config.js generated, uses `@import "tailwindcss"` in globals.css)
- ESLint version: 9.39.4 (flat config, eslint.config.mjs)
- `next lint` command REMOVED in Next.js 16 — use `eslint .` directly instead
- `@next/eslint-plugin-next` must be installed explicitly as a dev dependency (not auto-hoisted from eslint-config-next with pnpm)
- `create-next-app` interactive prompt has `--no-react-compiler --no-agents-md` flags to skip interactive prompts
- `create-next-app` won't run in directory with existing files — used temp dir + rsync
- lint-staged commands must use `pnpm exec eslint --fix` / `pnpm exec prettier --write` (not bare `eslint`/`prettier`)
- `prettier` must be installed as explicit dev dependency for lint-staged to find it
- Husky init worked as expected: yes (npx husky init creates .husky/pre-commit with npm test default)
- Playwright install success: yes (chromium only, --with-deps requires sudo)
- `next.config.ts` generated by create-next-app — replaced with `next.config.mjs` (ESM) as required
- tsconfig.json already had `"strict": true` generated by create-next-app

## [Task 2] Theme System Findings

- Press_Start_2P + VT323 loaded via next/font/google, CSS variables --font-pixel and --font-terminal
- Tailwind v4 @theme directive used for all custom tokens
- CSS classes available: .scanlines, .crt-vignette, .bevel-raised, .bevel-sunken, .neon-glow-pink, .neon-glow-green, .neon-glow-cyan, .neon-border
- Keyframes: neon-flicker, scanline-scroll
- prefers-reduced-motion: disables all animations
- Body defaults: background #0a0a0a, color #c0c0c0, font-family var(--font-terminal)

## [Task 5] Project Data Layer Findings

- ProjectMeta type: slug, title, description, tags[], image?, demoAvailable, url?, repo?
- Seed project with demoAvailable: true → slug: "pixel-canvas" (demo component expected at components/demos/pixel-canvas.tsx)
- getAllProjects() returns projects array from content/projects.ts
- getProjectBySlug(slug) finds by slug
- 3 seed projects total: pixel-canvas (demo), retro-terminal, neon-clock

## [Task 4] MDX Blog Infrastructure Findings

- @next/mdx + remark-frontmatter + remark-mdx-frontmatter = YAML frontmatter as named export `frontmatter`
- Plugin order: [remarkFrontmatter, remarkMdxFrontmatter, remarkGfm] then [rehypeSlug, [rehypePrettyCode, opts]]
- Dynamic import pattern: `const mod = await import(\`@/content/blog/${slug}.mdx\`)`
- frontmatter is at `mod.frontmatter`, component is at `mod.default`
- generateStaticParams: uses fs.readdirSync, NOT import — avoids circular import
- dynamicParams = false: ensures 404 for unknown slugs
- app/blog/[slug]/page.tsx: params is Promise<{slug}> in Next.js 16 App Router
- CRITICAL: next build uses Turbopack by default in Next.js 16 — @next/mdx plugin options (functions) are NOT serializable for Turbopack
- FIX: use `next build --webpack` flag to force webpack bundler (Turbopack does not support non-serializable loader options)
- Build script updated: `"build": "next build --webpack"` in package.json

## [Task 3] Navigation Shell Findings

- Navigation: 'use client' (needs usePathname hook), uses next/link
- data-testid values: nav-home, nav-projects, nav-blog, nav-resume, nav-contact
- Footer: data-testid="footer", server component
- HitCounter: 'use client' (needs useState/useEffect), data-testid="hit-counter"
- UnderConstruction: server component, data-testid="under-construction", 45deg stripe bg
- layout.tsx: scanlines on body, crt-vignette on main, Navigation at top, UnderConstruction at very top
- Font variables applied to <html>: ${pressStart2P.variable} ${vt323.variable}
- body style: backgroundColor #0a0a0a, color #c0c0c0

## [Task 8] RSS Feed Route Findings

- RSS route can be built as a static App Router handler with `export const dynamic = 'force-static'`
- `getAllPosts()` already provides slug + frontmatter, so RSS only needs blog posts, not projects
- `app/layout.tsx` already includes `alternates.types['application/rss+xml']`, so no metadata change was needed
- `pnpm build` succeeded after adding the route; TypeScript LSP diagnostics could not run because `typescript-language-server` is not installed in this environment
- **Tailwind v4 Setup:** Direct mapping of custom CSS properties (`@theme` variables) doesn't always automatically generate utilities like `text-retro-silver`. Sometimes using inline styles with CSS variables mapping `var(--color-neon-pink, #ff00ff)` is a safer fallback for explicit coloring when unsure of the Tailwind v4 utility name generation rule for custom `-` colors.
